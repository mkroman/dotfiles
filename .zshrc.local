# Copyright (C) 2016 Mikkel Kroman <mk@maero.dk>

alias irb='pry'
alias adt='addtorrent'
alias b='bat'
alias cat='bat -pp'

# Alias `vim` to `vimx` (vim enhanced) if it exists
if command -v vimx >/dev/null 2>&1; then
  # alias vim='vimx'
fi
